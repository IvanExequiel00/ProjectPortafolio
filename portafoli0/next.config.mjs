/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['github.com'], // Añade aquí los dominios de los que deseas cargar imágenes
    },
  };
  
  export default nextConfig;